function result = apply(model, data)
  % This function, model.blda.apply(), applies the given data on given
  % model. The model is actually a parameter structrure, obtained from
  % trainning function, model.blad.train(), for example.
  % INPUT
  %	 model -
  %	 data  -
  %
	 
end
